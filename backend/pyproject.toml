[project]
name = "reddalert-backend"
version = "0.1.0"
description = "Reddalert — Reddit monitoring and alerting service"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py311"
line-length = 100
