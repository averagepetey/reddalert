# Database
DATABASE_URL=postgresql://reddalert:reddalert@localhost:5432/reddalert

# JWT Authentication
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=

# Reddit API credentials
# Create a "script" type app at https://www.reddit.com/prefs/apps
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=reddalert/0.1

# Discord Bot (optional — enables "Connect to Discord" button)
# Create an application at https://discord.com/developers/applications
# Under Bot section, enable bot and copy token
# Under OAuth2, add redirect URL: http://localhost:3000/discord/callback
DISCORD_CLIENT_ID=
DISCORD_BOT_TOKEN=
DISCORD_REDIRECT_URI=http://localhost:3000/discord/callback

# Application
CORS_ORIGINS=http://localhost:3000
POLL_INTERVAL_MINUTES=5
RETENTION_DAYS=30
DEBUG=true
